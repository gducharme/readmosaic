{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://readmosaic/schemas/sentence_ids.schema.json",
  "title": "Pre-processed manuscript sentence records",
  "type": "object",
  "required": [
    "id",
    "order",
    "prev_id",
    "next_id",
    "text",
    "start_char",
    "end_char",
    "paragraph_id",
    "manuscript_id",
    "source"
  ],
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 1,
      "description": "Stable sentence identifier."
    },
    "order": {
      "type": "integer",
      "minimum": 0,
      "description": "Zero-based sentence order in the manuscript."
    },
    "prev_id": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Previous sentence ID, or null for the first sentence."
    },
    "next_id": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Next sentence ID, or null for the last sentence."
    },
    "text": {
      "type": "string",
      "description": "Sentence text."
    },
    "start_char": {
      "type": "integer",
      "minimum": 0,
      "description": "Start offset in the manuscript (inclusive)."
    },
    "end_char": {
      "type": "integer",
      "minimum": 0,
      "description": "End offset in the manuscript (exclusive)."
    },
    "paragraph_id": {
      "type": "integer",
      "minimum": 1,
      "description": "Parent paragraph identifier."
    },
    "manuscript_id": {
      "type": "string",
      "description": "Stable identifier for the manuscript or version."
    },
    "source": {
      "type": "string",
      "description": "Source path or URI for the manuscript."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp for when this artifact was produced."
    },
    "tokenization": {
      "type": "object",
      "description": "Metadata about the tokenizer or segmenter used to generate IDs.",
      "properties": {
        "method": {"type": "string"},
        "model": {"type": "string"},
        "notes": {"type": "string"}
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
