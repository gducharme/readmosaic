version: '3.9'
services:
  mosaic-terminal:
    build: .
    container_name: mosaic-terminal
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - DATA_DIR=/data
    volumes:
      - ./data:/data
